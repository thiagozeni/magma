# macOS
.DS_Store
.AppleDouble
.LSOverride

# Thumbnails
._*

# Node (caso venha a usar build tools)
node_modules/
npm-debug.log*
yarn-debug.log*

# Editor
.vscode/
.idea/
*.swp
*.swo

# Temp / backup
*.bak
*.bak-*
*.tmp
*~

# Protótipos / experimentos locais (não publicar)
_anime-proto/

# Security / Credentials
.env
.env.local
.env.*.local
service_account.json
*.pem

# Videos / large media (não publicados — pesam GB)
videos/
_divulga/
# video-fx/ É tracado: frames das animações scroll-scrub + MP4s do site (~14MB)
